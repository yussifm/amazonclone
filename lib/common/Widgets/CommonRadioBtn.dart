import "package:flutter/material.dart";

class CommonRadioBtn extends StatelessWidget {
  final String value;
  final groupValue;

  const CommonRadioBtn(
      {Key? key, required this.value, required this.groupValue})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
